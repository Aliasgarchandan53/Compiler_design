#include <bits/stdc++.h>
using namespace std;

int main(){
    fstream file;
    file.open("input.c");
    string word;
    //reads each stream of charcters space separated
    while(file>>word){
        for(int i=0;i<word.length();i++){
            
        }
    }
    return 0;
}